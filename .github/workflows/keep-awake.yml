name: Keep Streamlit Awake

on:
  schedule:
    - cron: "*/30 * * * *"   # her 30 dakikada bir
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Open Streamlit App
        run: |
          curl -L "https://ekonomi-dunyasi-yonetim-msu7zf86qlczbai2sb99am.streamlit.app" > /dev/null 2>&1
